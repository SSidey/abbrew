<div class="abbrew chat-message chat-card flexcol " data-actor-id="{{actor._id}}" data-message-id="{{messageId}}">
    <header>
        <h3 class="actor-name">{{actor.name}}</h3>
    </header>

    <div class="message-content">
        <div>{{actor.name}} lost the will to fight.</div>
    </div>
</div>