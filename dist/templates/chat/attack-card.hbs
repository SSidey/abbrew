<div class="abbrew chat-card attack-card" data-actor-id="{{actor._id}}" data-item-id="{{item._id}}" data-token-id="{{tokenId}}">
    <header>
        <h3 class="item-name">{{item.name}}</h3>
    </header>

    <div class="card-content">{{{item.system.description}}}</div>
</div>