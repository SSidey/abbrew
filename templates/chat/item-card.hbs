<div class="abbrew chat-card item-card" data-actor-id="{{actor._id}}" data-item-id="{{item._id}}">
    <header>
        <h3 class="item-name">{{item.name}}</h3>
    </header>

    <div class="card-content">{{{item.system.description}}}</div>

    <div class="card-buttons">
        <button data-action="damage">Damage</button>
    </div>
</div>